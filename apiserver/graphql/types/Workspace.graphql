type Workspace {
    id: ID!
    name: String!
    description: String
    slug: String!
    created_at: String!
    updated_at: String!
    channels: [Channel!]! @hasMany
    owner: User! @belongsTo
}
